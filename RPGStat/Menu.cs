namespace RPGStat
{
    public class Menu
    {
        private Controller _controller; // has access to the controller but have no idea why
        
        public enum MainMenu
        {
            Generate,
            Exit
        }
    }
}